# Tool Window Analyze

This code provide a way of analyzing the tool window duration.

For running the code, please run the command below:

```aiignore
python main.py
```

This code will generate 6 plots of the tool window duration  
and print down the statistic information of the tool window duration for data from the 'toolwindow_data.csv' file.